name: AVclass
version: 1
description: >
  Extracts malware family and details from AV labels

accepts: .*
rejects: empty|metadata/.*

# TODO Which stage to get MetaDefender and VirusTotal?
stage: 
category: 

file_required: false
timeout: 10
disable_cache: false

enabled: true
is_external: false
licence_count: 0

#heuristics:

docker_config:
  image: ${REGISTRY}testing/assemblyline-service-avclass:latest
  cpu_cores: 1.0
  ram_mb: 256
